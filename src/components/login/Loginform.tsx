import React from "react";
import { Header } from "../../common/commonstyle";

const Loginform = () => {
  return (
    <Header>
      <div>FINE</div>
      <div>
        <button>Home</button>
        <button>Friend Matching</button>
        <button>Restaurant review</button>
      </div>
      <div>
        <button>KOR</button>
        <button>Login</button>
      </div>
    </Header>
  );
};

export default Loginform;
